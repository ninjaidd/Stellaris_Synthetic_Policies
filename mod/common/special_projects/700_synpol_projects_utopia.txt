# Replaces FLESH_IS_WEAK_PROJECT
special_project = {
	key = "SYN_POL_CYBERNETICS_PROJECT"
	cost = {
		base = 1000
		scaled_modifier = {
			limit = {
			}
			scope = this
			add = 200
			calc = pop_count
		}
	}
	tech_department = engineering_technology
	icon = "gfx/interface/icons/situation_log/situation_log_main_quest.dds"
	picture = GFX_evt_robot_assembly_plant

	event_scope = planet_event

	requirements = {
	}

	on_success = {
		event_target:flesh_weak_owner = {
            # Calls synpolu.2 instead of utopia.2501
			country_event = { id = synpolu.2 }
		}
	}

	on_fail = {
	}
}

special_project = {
	key = "SYN_POL_SYNTHETICS_PROJECT"
	cost = {
		base = 1000
		scaled_modifier = {
			limit = {
			}
			scope = this
			add = 200
			calc = pop_count
		}
	}
	tech_department = engineering_technology
	icon = "gfx/interface/icons/situation_log/situation_log_main_quest.dds"
	picture = GFX_evt_robot_assembly_plant

	event_scope = planet_event

	requirements = {
	}

	on_success = {
		event_target:synth_evo_owner = {
            # Calls synpolu.4 instead of utopia.2551
			country_event = { id = synpolu.4 }
		}
	}

	on_fail = {
	}
}